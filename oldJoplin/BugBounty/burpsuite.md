burpsuite

pra fazer bypass em csfr

Project Options -> Sessions -> Macros (add) faz uma macro pra dar uma get na pagina

ainda em Sessions -> Session Handling Rules (add) crie um scopo somente pra ferramento e suite scope (target do site)

e crie uma rule pra usar o macro.

decoder

https://gchq.github.io/CyberChef/

%2500 poison null byte -> termina qualquer coisa na url